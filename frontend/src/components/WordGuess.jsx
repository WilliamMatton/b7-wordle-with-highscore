import { useState } from "react";
import Letter from "./Letter.jsx";

export default function WordGuess({ word }) {
  const [letters, setLetters] = useState([

  ]);

  for(let i = 0; i < word.length; i++) {
    
  }

  /*
  return (
    
  );
  */
}